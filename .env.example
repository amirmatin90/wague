# Local stub only. The API refuses to start if HL_NETWORK is not stub,
# or if HL_AGENT_KEY / HL_MASTER_KEY / WALLET_HOT_SEED_MAINNET are set.
HL_NETWORK=stub

DATABASE_URL=postgresql+psycopg://otc:otc_local@postgres:5432/otc
JWT_SECRET=local-dev-jwt-not-for-production
QUOTE_TTL_MS=20000
